import { createTheme } from "@mui/material/styles";

// eslint-disable-next-line no-unused-vars
export const drawnNumberTheme = createTheme({
	palette: {
		warning: {
			main: "#ffa500",
		}
	}
});



